'use strict';
angular.module('common.controllers')
    .controller('dashboardCtrl', function ($scope, $location, $timeout, $translate, $q, common, portalService, CONSTANTS) {
        if (_MODE_ == "DEBUG") console.log("dashboardControllers.js : dashboardCtrl");
        var contents = this;

    })
;
